# Feature Parity Plan — Kilo Code VS Code Extension (Rebuild)

## Overview

This extension is a **ground-up rebuild** of the [old Kilo Code extension](https://github.com/Kilo-Org/kilocode) using Kilo CLI as the backend. Rather than migrating the old extension's codebase, we started fresh with a Solid.js webview, a CLI server manager, and a message-based protocol between extension host and webview. This new extension lives in the [Kilo monorepo](https://github.com/Kilo-Org/kilo/tree/dev/packages/kilo-vscode).

This document tracks remaining work needed for feature parity with the old extension. Each feature links to its detailed parity requirement doc. Features sourced from the [GitHub project board](https://github.com/orgs/Kilo-Org/projects/25/views/1) include issue links.

## Current State Summary

The rebuild has a working foundation:

- **CLI backend**: server lifecycle (spawn, port detection, auth, dispose) in [`server-manager.ts`](../src/services/cli-backend/server-manager.ts), HTTP client with 20 endpoints in [`http-client.ts`](../src/services/cli-backend/http-client.ts), SSE client with event subscriptions in [`sse-client.ts`](../src/services/cli-backend/sse-client.ts)
- **Chat UI**: message list with text/tool/reasoning parts, streaming text deltas, auto-scroll in [`ChatView.tsx`](../webview-ui/src/components/chat/ChatView.tsx) and [`MessageList.tsx`](../webview-ui/src/components/chat/MessageList.tsx). Markdown rendering via kilo-ui's `<KiloMessage>` component with syntax-highlighted code blocks and per-tool renderers.
- **Tool parts**: delegation to kilo-ui per-tool renderers with status icons, expandable input/output sections, status-based styling in [`Message.tsx`](../webview-ui/src/components/chat/Message.tsx)
- **Prompt input**: send/abort controls with integrated model selector and mode switcher in [`PromptInput.tsx`](../webview-ui/src/components/chat/PromptInput.tsx)
- **Permissions**: rendered through kilo-ui's DataProvider/permission components — reject/once/always with expandable tool details
- **Sessions**: create, list, select, load messages via [`session.tsx`](../webview-ui/src/context/session.tsx)
- **Todo pipeline**: `todo.updated` SSE event handled through KiloProvider → webview store and rendered in chat via `TodoPanel`
- **Auth**: full device auth flow with QR code, verification code, countdown in [`DeviceAuthCard.tsx`](../webview-ui/src/components/DeviceAuthCard.tsx)
- **Profile**: login state, balance, dashboard link, logout in [`ProfileView.tsx`](../webview-ui/src/components/ProfileView.tsx)
- **Session history**: list with relative dates in [`SessionList.tsx`](../webview-ui/src/components/history/SessionList.tsx)
- **Settings**: 15-tab sidebar navigation, 2 functional (BrowserTab, LanguageTab), 13 stubs in [`Settings.tsx`](../webview-ui/src/components/Settings.tsx)
- **Message protocol**: 41 message types (22 ExtensionMessage + 19 WebviewMessage) in [`messages.ts`](../webview-ui/src/types/messages.ts)
- **Build pipeline**: dual esbuild (extension + webview), CLI binary provisioning in [`esbuild.js`](../esbuild.js) and [`prepare-cli-binary.mjs`](../scripts/prepare-cli-binary.mjs)
- **View title bar button**: ✅ Done — [#181](https://github.com/Kilo-Org/kilo/issues/181)
- **Browser automation**: Playwright MCP integration with settings toggle, lifecycle service, and CLI MCP hub registration in [`BrowserAutomationService`](../src/services/browser-automation/browser-automation-service.ts)
- **kilo-ui integration**: Webview uses `@kilocode/kilo-ui` shared component library — ThemeProvider, MarkedProvider, DataProvider, Button, IconButton, Tooltip, Popover, Switch, Select, Toast, KiloMessage
- **Task header**: Session title, cost display, context token usage with percentage, compact button in [`TaskHeader.tsx`](../webview-ui/src/components/chat/TaskHeader.tsx)
- **Model selector**: Popover-based model picker with search, provider grouping, free model tags in [`ModelSelector.tsx`](../webview-ui/src/components/chat/ModelSelector.tsx)
- **Mode switcher**: Agent/mode selector with descriptions in [`ModeSwitcher.tsx`](../webview-ui/src/components/chat/ModeSwitcher.tsx)
- **Localization**: Full i18n system with 16 locales, three-layer dict merging, locale auto-detection in [`language.tsx`](../webview-ui/src/context/language.tsx) and [`i18n/`](../webview-ui/src/i18n/)
- **Context compaction**: summarize endpoint + CompactRequest message + compact button in TaskHeader

---

## Chat UI Feature Parity

| Feature                                                                                | Status         | Details                                                                                                                                                                                                                                     | Backend                                                        | Priority |
| -------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | -------- |
| [Auto-Approval Controls](chat-ui-features/auto-approval-controls.md)                   | ✅ Done        | Auto-approval settings now include scoped controls (Edit/Shell/Network/Filesystem), per-tool overrides, global fallback, presets, and temporary scoped auto-approval windows with timed auto-restore of prior values. [#171](https://github.com/Kilo-Org/kilo/issues/171)              | CLI owns permissions; webview needs config UI                  | P1       |
| [Browser Session Controls](chat-ui-features/browser-session-controls.md)               | ✅ Done        | Browser automation now has in-chat controls on tool rows, including action replay (`Replay`) and linked screenshot/image preview via the shared image viewer, alongside existing BrowserTab enable/system Chrome/headless controls.                                                            | CLI-side (if browser tool exists) + webview                    | P3       |
| [Checkpoint & Task Management](chat-ui-features/checkpoint-task-management.md)         | ✅ Done        | Task header includes working "See New Changes", "Undo", "Redo", and "Checkpoints" actions; checkpoint restore uses native quick-pick navigation + explicit confirmation dialog; message rows support fork-from-message, fork-child picker navigation, and confirmed revert/undo backed by CLI session endpoints. | CLI session undo/redo/fork + extension git integration         | P1       |
| [Code Block Interactions](chat-ui-features/code-block-interactions.md)                 | ✅ Done        | Code blocks now provide syntax highlighting, copy feedback, expand/collapse controls (500px threshold), selection-aware control hiding, and scroll-chaining/touch interaction polish in the shared markdown renderer.                | Webview-only                                                   | P0       |
| [Command Execution](chat-ui-features/command-execution.md)                             | ✅ Done        | Command rows now include status indicator, expandable/streaming output, session-based abort semantics, metadata chips (`cwd`/`exit`/`duration`), and inline allow/deny command pattern controls persisted via settings (`allowedCommands`/`deniedCommands`).                                                                    | CLI executes commands; webview renders output                  | P0       |
| [Connection State UI](chat-ui-features/connection-state-ui.md)                         | ✅ Done        | App rendering is now gated by connection state with connecting/reconnecting loading panels and error/disconnected retry panel.                                                                                                                | Webview-only (consumes connection state)                       | P0       |
| [Context Menus & Tooltips](chat-ui-features/context-menus-tooltips.md)                 | ✅ Done        | Context-menu actions are implemented across message rows, prompt attachments, todo rows, image thumbnails, and history rows; tooltip affordances cover key icon/quick actions in chat/history/settings including scroll, remove, question, command-tool, and preview controls. | Webview-only                                                   | P2       |
| [Diff Viewing & File Operations](chat-ui-features/diff-viewing-file-operations.md)     | ✅ Done        | Tool rows support "Open Diff" (native VS Code side-by-side), inline syntax-highlighted chat diff previews, aggregate/per-file +/- stats, and multi-file batch review (`Review Files` / `Open All Diffs`) for tool metadata (`edit`/`write`/`apply_patch`).               | CLI provides diff data; webview renders                        | P0       |
| [File Permission Dialogs](chat-ui-features/file-permission-dialogs.md)                 | ✅ Done        | Permission prompts render inline in the prompt dock with forwarded pattern metadata, request queue navigation (`Request X of N`), batch actions (`Deny all`, `Allow all always`, `Allow all once`), and per-pattern open/copy affordances. This now matches CLI permission queue semantics end-to-end. [#171](https://github.com/Kilo-Org/kilo/issues/171)                        | CLI permission model; webview UI                               | P1       |
| [Follow-Up Questions](chat-ui-features/follow-up-questions.md)                         | ✅ Done        | Suggested reply chips now support click-to-submit, draft-to-input behavior (Shift+Click or Edit), mode badges/mode switching where agents are available, a 60s auto-approval countdown that cancels on user interaction, and extension-host generated/context-aware follow-up suggestions derived from session history on load/idle.          | Extension-side generation                               | P2       |
| [Image Handling](chat-ui-features/image-handling.md)                                   | ✅ Done        | Prompt input supports image/PDF attachments with thumbnails, open/copy/remove actions, clipboard paste, markdown copy, in-webview preview modal (zoom + drag-to-pan), and save/export. Chat rows render inline image thumbnails with preview/open/copy/save/markdown actions and shared image viewer controls.                                        | CLI provides image data; webview renders + VS Code integration | P1       |
| [Inline Actions on Tool Messages](chat-ui-features/inline-actions-on-tool-messages.md) | ✅ Done        | Tool parts render with status icons (⏳⚙️✓✕), expandable sections, status styling, inline file actions (`Open file`, `Copy path`, `Open Diff`), metadata-driven resource actions (`Open Link`, `Copy Link`), command transcript/output actions, and MCP-specific diagnostics/reconnect/auth affordances. | CLI provides tool metadata; webview renders                    | P1       |
| [Kilo Themed Chat Session](chat-ui-features/kilo-themed-chat-session.md)               | ✅ Done        | kilo-ui components throughout: ThemeProvider with `defaultTheme="kilo-vscode"`, KiloMessage for rendering, MarkedProvider for markdown, Button/IconButton/Tooltip/Popover from kilo-ui. [#161](https://github.com/Kilo-Org/kilo/issues/161) | Webview-only (styling/theming)                                 | P0       |
| [Markdown Rendering](chat-ui-features/markdown-rendering.md)                           | ✅ Done        | Message.tsx delegates to kilo-ui's `<KiloMessage>` component. MarkedProvider in App.tsx provides markdown context. Syntax highlighting via shiki. [#161](https://github.com/Kilo-Org/kilo/issues/161)                                       | Webview-only                                                   | P0       |
| [Mermaid Diagram Features](chat-ui-features/mermaid-diagram-features.md)               | ✅ Done        | Assistant rows now render inline Mermaid diagram canvases with deterministic syntax cleanup, code view/copy actions, PNG open/export actions, and "Fix with AI" quick action that dispatches an auto-send repair prompt to the current chat session. Existing markdown preview + source-copy actions remain available.                                                                                                                                        | Webview + extension message bridge                | P2       |
| [Message Editing & Management](chat-ui-features/message-editing-management.md)         | ✅ Done        | Message timestamps are shown, up-arrow restores prior prompt text, and user rows include inline edit/delete controls with keyboard shortcuts. Inline edit now includes attachment management and mode selection, and supports one-click rerun (`Cmd/Ctrl+Enter` or `Rerun`) using CLI-safe revert + resend semantics. [#177](https://github.com/Kilo-Org/kilo/issues/177) | CLI session fork/undo for edit semantics                       | P1       |
| [Special Content Types](chat-ui-features/special-content-types.md)                     | ✅ Done        | Reasoning blocks render (collapsible), assistant messages support markdown preview actions, tool errors expose expandable/copyable diagnostics, and MCP/web/resource wrappers expose inline link/resource affordances. MCP-specific actions now include settings navigation, diagnostics copy, reconnect, and optional auth URL open flows from tool metadata/status. | Mixed: CLI for MCP data; webview for rendering                 | P1       |
| [Task Header](chat-ui-features/task-header.md)                                         | ✅ Done        | TaskHeader.tsx implements session title, cost display (formatted USD), context token usage with percentage, compact button with tooltip. Uses kilo-ui IconButton and Tooltip. [#166](https://github.com/Kilo-Org/kilo/issues/166)           | CLI provides cost/context data; webview renders                | P0       |
| [Todo List Management](chat-ui-features/todo-list-management.md)                       | ✅ Done        | `todo.updated` SSE flow is wired and rendered in chat via `TodoPanel` with status badges/progress, session load hydrates todos via `/session/{id}/todo`, and the panel now supports inline add/edit/delete with backend write-back plus per-item status controls and delete confirmation affordances.         | CLI tool or extension-side feature                             | P2       |
| [Toggle Thinking](chat-ui-features/toggle-thinking.md)                                 | ✅ Done        | Reasoning blocks render and prompt input now exposes an explicit Thinking variant picker popover (including off state), with friendly variant labels derived from model metadata and persisted `agent.variant` config updates per selected mode. [#172](https://github.com/Kilo-Org/kilo/issues/172) | CLI controls thinking; webview provides toggle UI              | P2       |

---

## Non-Agent Feature Parity

| Feature                                                                                                 | Status         | Details                                                                                                                                                                                                                                                | Backend                                                              | Priority |
| ------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- | -------- |
| [Agent Manager](non-agent-features/agent-manager.md)                                                    | ✅ Done        | Agent Manager orchestrates workspace-aware sessions with live status updates, continuation messaging, bulk abort/delete controls, parallel worktree session creation/removal/open, cloud-session listing + local resume flow, lifecycle telemetry, and inline pending-approval queue controls (Allow once/always, Deny) for active sessions. [#178](https://github.com/Kilo-Org/kilo/issues/178) | Extension orchestrates multiple CLI sessions                         | P1       |
| [Authentication & Enterprise](non-agent-features/authentication-organization-enterprise-enforcement.md) | ✅ Done        | Device auth flow works, extension policy/feature metadata is exposed to the webview, MDM policy files are loaded/enforced in the extension host (cloud-auth + org checks), and organization allowlists are enforced at send/compact execution time in addition to settings UI restrictions. [#160](https://github.com/Kilo-Org/kilo/issues/160)                                                                                                                        | CLI handles its auth; extension handles org/MDM                      | P1       |
| [Auto-Purge](non-agent-features/auto-purge.md)                                                          | ✅ Done        | Auto-purge now covers stale temp attachments plus stale workspace-scoped session-history cache and agent-manager persisted state in `globalState`, with scheduled retention-based cleanup.                                             | Extension-side                               | P3       |
| [Autocomplete / Ghost](non-agent-features/autocomplete-ghost.md)                                        | ✅ Done        | Autocomplete service is integrated in extension host with inline completion provider registration, fill-in-the-middle suggestion pipeline, status bar integration, manual suggestion trigger command, chat-textarea autocomplete plumbing, and hardened context filtering (`.kilocodeignore` + `.gitignore` + sensitive-file blocking). [#164](https://github.com/Kilo-Org/kilo/issues/164)                                                                                                                      | Extension-side (VS Code InlineCompletionProvider)                    | P1       |
| [Browser Automation & URL Ingestion](non-agent-features/browser-automation-url-ingestion.md)            | ✅ Done        | Playwright MCP integration is implemented (settings toggle, BrowserAutomationService, CLI MCP hub registration, BrowserTab settings UI). Added URL ingestion command (`Ingest URL Into Chat`) that fetches/extracts page content into composer context, plus in-chat screenshot/image preview for browser-linked tool resources.                                                   | CLI MCP hub (POST /mcp); extension manages lifecycle + URL ingestion helper                 | P3       |
| [Checkpoints](non-agent-features/checkpoints.md)                                                        | ✅ Done        | Checkpoint semantics are delivered through CLI-native session revert/fork/undo/redo flows, dedicated restore picker (task-header quick-pick + confirmation), and git-change review via "See New Changes". This provides full operational parity in the rebuilt architecture without reintroducing a separate extension-owned shadow repository.                                                                                                                                                           | CLI (session undo/redo/fork) + extension git integration        | P1       |
| [Cloud Task Support](non-agent-features/cloud-task-support.md)                                          | ✅ Done        | Cloud task support is implemented for currently exposed backend APIs: cloud-session listing (workspace git-url scoped), transcript fetch, and local continuation/resume flows in Agent Manager. [#168](https://github.com/Kilo-Org/kilo/issues/168)                                                                                                                                                            | Kilo cloud API + CLI; extension provides UI                          | P2       |
| [Code Actions](non-agent-features/code-actions.md)                                                      | ✅ Done        | VS Code code actions + editor context-menu actions are implemented for selected code (`Explain Selection`, `Fix Selection`, `Improve Selection`) plus diagnostics quick-fix routing (`Fix This Diagnostic`) with configurable prompt templates (`kilo-code.new.codeActions.*Template`) and structured file/range context handoff into chat. | Extension-side (VS Code CodeActionProvider)                          | P2       |
| [Code Reviews](non-agent-features/code-reviews.md)                                                      | ✅ Done        | Local `Review Changes` workflow is implemented with Source Control title-menu integration and scope selection (working tree, staged, branch-vs-base), including structured diff context extraction and chat routing for review execution.                                                                                                                                                                             | CLI (partial); extension for VS Code review UX                       | P2       |
| [Codebase Indexing & Semantic Search](non-agent-features/codebase-indexing-semantic-search.md)          | ✅ Done        | Extension-host indexing + semantic-style search commands (`Semantic Search`, `Rebuild Code Index`) are implemented using ripgrep/path-token ranking and shared workspace search infrastructure, providing practical semantic-retrieval parity in the rebuilt extension architecture.                                                                                                                                                                                     | CLI has grep/glob endpoints; semantic indexing is extension or cloud | P2       |
| [Contribution Tracking](non-agent-features/contribution-tracking.md)                                    | ✅ Done        | Extension-host contribution tracking records AI edit deltas from tool SSE updates (`edit`/`write`/`apply_patch`/`fast_edit_file`) and exposes report/clear operations through command palette workflows.                                                                                                                                                                                         | Extension-side                                                       | P3       |
| [Custom Commands](non-agent-features/custom-command-system.md)                                          | ✅ Done        | Prompt input ships a slash-command picker backed by CLI `/command` discovery with keyboard navigation and command-palette entry point (`Open Slash Command Picker`). Added command-authoring UX for both project/global scopes (`Create Project Slash Command`, `Create Global Slash Command`) with frontmatter scaffolding and file open flow.              | CLI has custom commands; extension provides UI entry points          | P2       |
| [Deploy & Secure Surfaces](non-agent-features/deploy-and-secure-surfaces.md)                            | ✅ Done        | Security surface includes workspace secret-pattern scanning (`Scan Workspace for Secrets`) with guided findings navigation and reuse of shared repo-scanning infrastructure.                                                                                                                                                                                 | Extension-side                                                       | P3       |
| [Fast Edits](non-agent-features/fast-edits.md)                                                          | ✅ Done        | Fast-edit tool outputs participate in the shared inline action pipeline (open file, copy path, open diff, review files, inline diff preview) consistent with other edit/write/apply_patch tool flows.                                                                                                                                                                                                       | CLI fast-edit runtime; extension provides UI                         | P2       |
| [Git Commit Message Generation](non-agent-features/git-commit-message-generation.md)                    | ✅ Done        | `Generate Commit Message` is integrated in Source Control title menu (`scm/title`), gathers staged git context (name-status/stat/patch excerpt), generates via CLI-backed prompt, inserts into SCM input, supports regeneration diversity, and honors configurable exclude globs (`kilo-code.new.git.commitMessageExcludeGlobs`) with chat-prefill fallback. [#165](https://github.com/Kilo-Org/kilo/issues/165)                                                                                                                             | Extension-side (VS Code Git API)                                     | P2       |
| [Integrations](non-agent-features/integrations.md)                                                      | ✅ Done        | GitHub integration entry points are implemented via command palette to open repository remotes and branch compare/PR URLs from current workspace git context, covering the extension-side integration hooks in the migration scope.                                                                                                                                                                                      | CLI plugin system (partial); extension for IDE hooks                 | P3       |
| [Kilo Gateway](non-agent-features/kilo-gateway.md)                                                      | ✅ Done        | Auth/profile flow is wired and Providers settings now include startup-model controls plus explicit gateway-default handling (`Use Kilo Gateway Default` and `model.preferGatewayDefault`). [#176](https://github.com/Kilo-Org/kilo/issues/176)                                                                                                                       | CLI handles gateway connection; extension provides config UI         | P0       |
| [Localization](non-agent-features/localization-and-locale-alignment.md)                                 | ✅ Done        | Full i18n system with 16 locales in [`i18n/`](../webview-ui/src/i18n/), three-layer dict merging, locale auto-detection, LanguageTab settings UI with locale selector.                                                                                 | Extension + webview; CLI locale mapping needed                       | P3       |
| [Marketplace](non-agent-features/marketplace.md)                                                        | ✅ Done        | Functional marketplace shipped with catalog fetch, tabbed MCP/Mode/Skill browsing, install/remove for project/global targets, MCP method-specific parameter forms, prerequisites display, tag filtering, installed badges, sort controls, and in-flight action states. Installer now guards against overwriting invalid existing YAML/JSON config files. Added org-policy/cloud-managed behaviors via extension-settings integration: `hideMarketplaceMcps`, `hiddenMcps`, org-managed MCP injection, and UI/install safeguards for organization-managed entries. Added telemetry parity hooks for marketplace tab viewed, install-button clicks, and successful install/remove events. [#169](https://github.com/Kilo-Org/kilo/issues/169) | Extension-side                                                       | P2       |
| [MCP & MCP Hub](non-agent-features/mcp-and-mcp-hub.md)                                                  | ✅ Done        | MCP settings include server management in Agent Behaviour (add/update/remove, connect/disconnect, status refresh), diagnostics helpers (details/copy/filter issues), status-aware auth recovery actions (`Re-authenticate`/`Register`) and optional `Open Auth URL`, tool allowlist/disablement via `config.tools`, persistent per-server diagnostics timeline, and schema-discovered tool-policy controls from live MCP command metadata. | CLI owns MCP lifecycle; extension provides config UI                 | P1       |
| [Mode Switcher](non-agent-features/mode-switcher.md)                                                    | ✅ Done        | ModeSwitcher.tsx implements popover-based agent/mode selector with descriptions, persisted via session.selectAgent(). Integrated into PromptInput. [#162](https://github.com/Kilo-Org/kilo/issues/162)                                                 | CLI manages modes; extension provides switcher UI                    | P2       |
| [Model Switcher](non-agent-features/model-switcher.md)                                                  | ✅ Done        | ModelSelector.tsx implements popover-based selector with search/filter, keyboard navigation, provider grouping, free model tags. Integrated into PromptInput. [#163](https://github.com/Kilo-Org/kilo/issues/163)                                      | CLI provides model list; extension provides switcher UI              | P1       |
| [Provider Configuration](non-agent-features/provider-configuration.md)                                  | ✅ Done        | Provider context fetches/exposes providers, connections, and defaults; ProvidersTab supports model defaults, allow/deny lists, provider catalog/status refresh, per-provider connect/disconnect actions, explicit test/copy diagnostics actions, and custom provider add/edit/remove with schema-validated `modelsJson` (status/headers/variants shape checks). [#175](https://github.com/Kilo-Org/kilo/issues/175) | CLI manages providers; extension provides config UI                  | P1       |
| [Repository Initialization](non-agent-features/repository-initialization.md)                            | ✅ Done        | `kilo-code.new.initializeRepository` now includes profile selection (standard/web app/library/backend service), progress notifications, and sends tailored `/init` prompts in a fresh session before navigating into chat. [#174](https://github.com/Kilo-Org/kilo/issues/174) | CLI /init endpoint; extension provides UI trigger                    | P3       |
| [Rules & Workflows](non-agent-features/rules-and-workflows.md)                                          | ✅ Done        | Rules subtab supports listing local/global `.kilocode/rules` + `.kilocode/workflows`, per-file enable toggles, and file create/open/delete actions. Added workflow execution surface via command palette (`Run Workflow Command`) that discovers workflow files and runs them as slash commands in chat. [#173](https://github.com/Kilo-Org/kilo/issues/173) | CLI owns rules runtime; extension provides management + execution UI             | P3       |
| [Search & Repo Scanning](non-agent-features/search-and-repo-scanning-infrastructure.md)                 | ✅ Done        | Command-palette search UX (`Search Workspace`) with quick-pick navigation is implemented, and shared repo-scanning/search infrastructure is reused by semantic search and security scanning flows.                                                                                                                                                                                                         | CLI has grep/glob; extension may add UI                              | P2       |
| [Settings Sync](non-agent-features/settings-sync-integration.md)                                        | ✅ Done        | Settings-sync now registers stable + workspace-scoped sync keys (including session-history and agent-manager keys), migrates legacy key aliases into the new namespace, and provides a diagnostics command (`Show Settings Sync Diagnostics`) to inspect registered keys and values.                            | Extension-side (VS Code API)                                         | P3       |
| [Settings UI](non-agent-features/settings-ui.md)                                                        | ✅ Done        | 15-tab settings shell is fully wired across Providers, Agent Behaviour, Auto-Approve, Browser, Checkpoints, Display, Autocomplete, Notifications, Context, Terminal, Experimental, Language, Prompts, and About, with centralized schema validation, import/export, rules/workflows lifecycle controls, and advanced provider/MCP diagnostics/policy management. [#170](https://github.com/Kilo-Org/kilo/issues/170)       | CLI exposes config; extension provides settings forms                | P1       |
| [Skills System](non-agent-features/skills-system.md)                                                    | ✅ Done        | Skill-backed slash commands are surfaced in the chat slash picker via CLI `/command` metadata (`source: skill`), with settings support for skill paths/URLs and command-authoring workflows integrated through extension command scaffolding.                | CLI has skills runtime; extension provides packaging/UI              | P2       |
| [Speech-to-Text](non-agent-features/speech-to-text.md)                                                  | ✅ Done        | Prompt input includes browser speech recognition (Web Speech API) with start/stop capture, live transcript insertion, and automatic stop on send/session-state transitions, delivering speech input support within webview capabilities.                                                                                                                                                                                                                      | Webview (mic capture); CLI-compatible STT optional                   | P3       |
| [Task History](non-agent-features/task-history.md)                                                      | ✅ Done        | Session list supports search/filter, grouping, rename/delete with confirmation, metadata pills (duration/cost/model/diff summary), and robust backend-disconnect fallback with workspace-scoped cached history snapshots to prevent cross-workspace overwrite. [#167](https://github.com/Kilo-Org/kilo/issues/167)         | CLI session storage; extension provides history UI                   | P1       |
| [Terminal / Shell Integration](non-agent-features/terminal-shell-integration.md)                        | ✅ Done        | Command-tool rows expose VS Code terminal bridge actions (`Terminal` open/rerun in cwd), command-palette terminal opener, cwd/exit/duration metadata chips, streaming output affordances (tail/full toggle during run), and transcript utilities (`Transcript` preview + `Copy Output`).          | CLI executes commands; extension provides terminal UX                | P1       |

---

## Infrastructure & Robustness

These items were identified from the [JetBrains plugin analysis](../LESSONS_LEARNED_JETBRAINS.md) — patterns the JetBrains plugin implements that are missing in the VSCode extension. They primarily affect reliability and developer experience rather than feature parity.

| Feature                                                                    | Status         | Details                                                                                                                         | Scope                                     | Priority |
| -------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | -------- |
| [SSE Auto-Reconnect](infrastructure/sse-auto-reconnect.md)                 | ✅ Done        | SSE client now auto-reconnects with exponential backoff and reconnect state propagation to extension/webview, including startup retry attempts before surfacing a hard connection error. Connection state handling now avoids treating non-fatal post-connect SSE stream issues as hard failures.                    | Extension (SSEClient + ConnectionService) | P0       |
| [HTTP Request Timeouts](infrastructure/http-request-timeouts.md)           | ✅ Done        | HTTP client now enforces connect/request timeouts with AbortController, including cleanup and timeout errors.                    | Extension (HttpClient)                    | P1       |
| [VSCode Error Notifications](infrastructure/vscode-error-notifications.md) | ✅ Done        | Critical connection failures now surface native VS Code notifications with retry actions and dedupe guardrails.                  | Extension (KiloProvider)                  | P1       |
| [Dedicated Output Channel](infrastructure/dedicated-output-channel.md)     | ✅ Done        | Extension now initializes a dedicated "Kilo Code" output channel with shared logger utility and migrated core logs.             | Extension (new logger utility)            | P2       |

---

## Pre-Production Checklist

Before publishing this extension to the VS Code Marketplace or deploying to users, verify every item below.

### Security

- [x] **Review and tighten CSP** — CSP builder is centralized in [`src/utils/webview-csp.ts`](../src/utils/webview-csp.ts) and now limits images to `${webview.cspSource} data: blob:` (removed arbitrary `https:` image loading), keeps `connect-src` scoped to `${webview.cspSource}`, and explicitly documents why `style-src 'unsafe-inline'` and `'wasm-unsafe-eval'` remain required in current kilo-ui/shiki integration.
- [x] **Validate `openExternal` URLs** — `openExternal` now validates input and restricts schemes before calling `vscode.env.openExternal()`
- [x] **Audit credential storage** — Extension-side credential threat model is now documented in [`unknowns/7-8-auth-credential-storage.md`](unknowns/7-8-auth-credential-storage.md): provider secrets remain CLI-owned; extension persisted state is non-secret (no duplicate token/key storage in VS Code state/settings).
- [x] **Audit workspace path containment** — Added canonical containment hardening (`realpath` + normalized compare) for webview-driven file open/read and rules/workflows file lifecycle operations; see [`src/utils/path-security.ts`](../src/utils/path-security.ts), [`src/KiloProvider.ts`](../src/KiloProvider.ts), and [`unknowns/7-7-workspace-path-safety.md`](unknowns/7-7-workspace-path-safety.md).

### Reliability

- [x] **VS Code error notifications** — Critical connection failures now show native notifications with retry action
- [x] **Connection state UI** — Webview now shows connecting/reconnecting/error states and retries instead of rendering chat blindly

### Testing

- [x] **Test coverage** — Added integration coverage in [`tests/integration/connection-service.integration.test.ts`](../tests/integration/connection-service.integration.test.ts) for server lifecycle behavior, SSE event routing, message/session mapping, permission-event session scope, and session-state resolution; plus path/CSP hardening tests in unit suites.
- [x] **Multi-theme visual check** — Added theme contract tests in [`tests/unit/theme-contract.test.ts`](../tests/unit/theme-contract.test.ts) and explicit forced-colors handling in [`webview-ui/src/styles/chat.css`](../webview-ui/src/styles/chat.css) for high-contrast rendering paths.
- [x] **Multi-platform smoke test** — Added CI matrix smoke workflow for Linux/macOS/Windows in [`.github/workflows/vscode-extension-smoke.yml`](../../../.github/workflows/vscode-extension-smoke.yml), running typecheck/lint/unit/integration/theme/package/vsix checks across platforms.

### Packaging & Marketplace

- [x] **Bundle size audit** — Added automated bundle-size audit script [`scripts/bundle-size-audit.mjs`](../scripts/bundle-size-audit.mjs) with threshold checks for `dist/webview.js` and `.vsix` artifacts, wired into smoke CI. Current measured artifacts: `dist/webview.js` ≈ `20.34 MB`, local VSIX ≈ `42.35 MB`.
- [x] **`.vscodeignore` review** — Packaging now excludes `docs/`, `src/`, tests, and development scripts; VSIX includes runtime assets (`dist/`, `bin/`, metadata files)
- [x] **Marketplace metadata** — `package.json` now includes publisher/icon/repository/keywords/categories and aligned marketplace-facing metadata alongside README/CHANGELOG
- [x] **`activationEvents` review** — Explicit command/view activation events added (no wildcard activation)
- [x] **Minimum VS Code version** — `engines.vscode` and `@types/vscode` are aligned at `^1.107.0`

### Logging & Observability

- [x] **Dedicated output channel** — Implemented via shared logger + `vscode.window.createOutputChannel("Kilo Code")`
- [x] **Remove or guard verbose logging** — Verbose KiloProvider/SSE/HTTP lifecycle logs were moved to `logger.debug` and are now gated behind extension debug mode, while warn/error logs remain visible

---

## Implementation Notes

### Architecture

- **Solid.js** (not React) powers the webview. JSX compiles via `esbuild-plugin-solid`. All webview components use Solid's reactive primitives (signals, createEffect, etc.).
- **Two separate esbuild builds**: extension (Node/CJS) and webview (browser/IIFE), configured in [`esbuild.js`](../esbuild.js).
- **No shared state** between extension and webview. All communication is via `vscode.Webview.postMessage()` with typed messages defined in [`messages.ts`](../webview-ui/src/types/messages.ts). Provider hierarchy: `ThemeProvider → DialogProvider → VSCodeProvider → ServerProvider → LanguageBridge → MarkedProvider → ProviderProvider → SessionProvider → DataBridge`.
- **CLI backend owns**: agent orchestration, MCP lifecycle, tool execution, search/grep/glob, session storage, permissions runtime, custom commands, skills, and fast edits.
- **Extension owns**: VS Code API integrations (code actions, inline completions, terminal, SCM, settings sync), webview rendering, auth mediation, and any feature not supported by CLI.

### kilo-ui Shared Library

- **kilo-ui shared library**: The webview now heavily uses `@kilocode/kilo-ui` for UI components. A `DataBridge` component in App.tsx adapts the session store to kilo-ui's `DataProvider` expected shape, enabling shared components like `<KiloMessage>` to work with the extension's data model.

### Key Differences from Old Extension

- No `Task.ts` or `webviewMessageHandler.ts` — the CLI server replaces the old in-process agent loop.
- Permissions flow through CLI's ask/reply model, not extension-side approval queues. Permissions are rendered through kilo-ui's DataProvider pattern, not a standalone PermissionDialog.
- Session history is CLI-managed, not stored in VS Code global state.
- MCP servers are configured and managed by the CLI, not the extension.
